{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build sync file",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g", "SyncFiles.cpp","-lstdc++fs"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}